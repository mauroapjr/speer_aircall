import React from "react";

const AllPhoneCallsPage = ({ calls }) => {
  // Implement the rendering logic for all phone calls here
  // You can use the provided 'calls' prop to access the data
  return (
    <div>
      {/* Your rendering logic for all phone calls */}
    </div>
  );
};

export default AllPhoneCallsPage;